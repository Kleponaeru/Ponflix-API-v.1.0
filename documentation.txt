# Anime API Project Context

## Goal

Build a clean, provider-based Anime REST API using **Next.js 16 App Router + TypeScript**.

The API should expose a consistent JSON format while scraping data from anime providers. We originally started with Kuramanime but switched to **Kuronime** because Kuramanime blocks production/Vercel requests (403 Forbidden).

---

# Current Provider

Current provider:

* Kuronime
* Base URL:

  ```ts
  export const BASE_URL = "https://kuronime.sbs";
  ```

All scraping currently targets Kuronime.

---

# Project Structure

```
src/
├── app/
│   └── api/
│       ├── latest/
│       └── search/
│
├── lib/
│   ├── providers/
│   │   └── kuronime/
│   │       ├── client.ts
│   │       ├── latest.ts
│   │       ├── search.ts
│   │       └── parser/
│   │           ├── card.ts
│   │           ├── latest.ts
│   │           └── search.ts
│   │
│   └── utils/
│       ├── number.ts
│       └── url.ts
│
└── types/
    ├── anime.ts
    ├── latest.ts
    ├── search.ts
    ├── episode.ts
    └── response.ts
```

---

# Architecture

The project follows a strict separation of responsibilities.

## client.ts

Responsible only for HTTP requests.

Example:

```ts
const html = await kuronimeClient.get("/");
```

No parsing logic belongs here.

---

## latest.ts / search.ts

Responsible only for:

1. Fetch HTML
2. Pass HTML into parser

Example:

```ts
const html = await kuronimeClient.get(...);

return parseLatest(html);
```

No scraping logic should exist here.

---

## parser/

Responsible only for converting HTML into typed objects.

Example:

```
HTML
 ↓
Cheerio
 ↓
LatestAnime[]
```

Parsers never perform HTTP requests.

---

# Shared Card Parser

A reusable parser exists:

```
parser/card.ts
```

It extracts fields shared by multiple pages:

* slug
* url
* path
* thumbnail

It intentionally does **NOT** parse:

* title
* episode
* score
* views

Those are page-specific and handled inside each parser.

---

# Types

Shared base type:

```ts
AnimeBase
```

Contains:

* slug
* title
* path
* url
* thumbnail

Other files extend AnimeBase:

```
LatestAnime
SearchAnime
Episode
AnimeDetails (later)
```

---

# Latest Endpoint

Endpoint:

```
GET /api/latest
```

Scrapes:

```
https://kuronime.sbs/
```

Parses:

* title
* slug
* thumbnail
* url
* path
* currentEpisode
* totalEpisodes (currently null)
* type (currently null)
* quality (currently null)
* hot (currently false)
* views
* timeAgo

Uses:

```
parseLatest()
```

---

# Search Endpoint

Endpoint:

```
GET /api/search?q=naruto
```

Scrapes:

```
https://kuronime.sbs/?s=<query>
```

Parses:

* title
* slug
* thumbnail
* url
* path
* type
* score

Uses:

```
parseSearch()
```

---

# Utilities

Current helpers:

```
parseNumber()
```

Converts values like:

```
761.228
```

into

```
761228
```

Additional helpers can be added under:

```
lib/utils
```

---

# Coding Style

Important project rules:

* Keep functions small.
* No duplicated parsing.
* Reuse parser/card.ts whenever possible.
* Services never parse HTML.
* Parsers never perform HTTP requests.
* Prefer readable code over clever code.
* Fix architecture before adding features.
* Avoid temporary hacks that require later refactoring.
* Make changes cleanly the first time.

---

# Current Status

Completed:

* Provider architecture
* HTTP client
* Shared card parser
* Latest endpoint
* Search endpoint
* Base types
* Response types
* Utility functions

Not started yet:

* Anime Details
* Episode List
* Episode Details
* Streaming Sources

When continuing this project, focus on maintaining the current architecture instead of restructuring it again unless absolutely necessary.
